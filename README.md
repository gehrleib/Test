identified a discrepancy between the validation file's active count (25,437) and the number currently active within our system (23,334). We suspect this difference arose because our account may not have had the necessary access permissions when CUBE initially created some RegGroups. If access was granted after creation, these groups likely wouldn't appear in our data calls, as their 'last updated' timestamp wouldn't have changed. To correct this, a full data synchronization ('full pull') will likely be required sometime before onboarding is complete. This issue should primarily affect groups created by CUBE during this setup phase; groups we create ourselves should synchronize correctly as we'll have immediate access."
