there can be a delay of up to one hour after validation is completed before mapping can be performed.

This interval exists because an automated job runs first to check for any significant changes compared to a previous validation. For example, a change in the regulatory theme might necessitate the removal of an assessable unit before mapping can proceed.

Typically, this delay doesn't cause issues as the individuals performing the validation and the mapping are usually different.

The item should appear under "Requires Mapping/Attestation" shortly.
