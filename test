These are supplemental plans submitted outside of the regular master plan cycle. They allow for sending controls to CALMA that weren't included in the original master plan. This might happen if a control was added later or if it's optional but desired, like an override.
