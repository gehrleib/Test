By allowing 'bulk requests,' it is essential to ensure that sections from issuance is selected. This is important because a validator can only initiate validation from sections to which they have validator access. However, with bulk requests, they might select sections from the entire inventory. The system allows users to 'lookup' any records for which they have at least read access. Since all users have at least read access to the entire inventory, this needs to be carefully managed.
