# In fetch_section_details method, inside the for loop

                # Check for the error case first and exit the current loop iteration.
                if isinstance(result_or_exc, Exception):
                    final_processed_sections_list.append(section_stub)
                    continue
                
                # --- This code is now guaranteed to be safe ---
                # We use `cast` to explicitly tell Pylance that `result_or_exc` is now
                # a dictionary, resolving the first "Cannot access attribute 'get'" error.
                previous_section_data_wrapper = cast(dict, result_or_exc)
                raw_previous_content = None

                # --- CORRECTED LOGIC START ---
                # First, safely get the inner 'section' dictionary from the wrapper.
                section_dict = previous_section_data_wrapper.get("section")

                # Next, check if that inner dictionary exists and is a valid dictionary.
                if isinstance(section_dict, dict):
                    # Now Pylance knows that section_dict is not None, so it is safe
                    # to call .get("content") on it.
                    raw_previous_content = section_dict.get("content")
                # --- CORRECTED LOGIC END ---

                normalized_previous_content = self._normalize_html_content(raw_previous_content)
                normalized_current_content = section_stub["content"]

                if normalized_previous_content and normalized_current_content != normalized_previous_content:
                    try:
                        section_stub["contentHtmlDiff"] = self._generate_placeholder_diff(
                            normalized_previous_content,
                            normalized_current_content,
                            current_section_id_for_logging,
                        )
                    except Exception as diff_error:
                        logging.error(f"Error during diff generation for section {current_section_id_for_logging}: {diff_error}")
                
                final_processed_sections_list.append(section_stub)
